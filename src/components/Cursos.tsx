import React from 'react';

export default function Cursos() {
  return (
    <section id="cursos" aria-label="Cursos" className="section container">
      <h2>Cursos</h2>
      <p>Lista de cursos (placeholder) — você pode preencher depois.</p>
    </section>
  );
}
